export interface GamuxProjectPageData {
    description?: string
    banner?: string
    externalLinks?: Record<string, string>
    screenshots?: string[]

    bgColor: string
    bg2Color: string
    textColor: string
    linkColor: string
    headingColor: string
}